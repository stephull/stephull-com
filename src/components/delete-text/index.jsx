import React from 'react';

import { API } from 'aws-amplify';
import { API_NAME } from '../../config';
import { deleteProjectText, deleteResumeText } from '../../endpoints';

const DeleteText = ({ function: funct }) => {

    return (
        <>
        </>
    )
}

export default DeleteText;